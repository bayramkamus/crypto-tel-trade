# Configuration

Secrets are stored in `.env`. Do not commit `.env`.

Telegram channels are stored in `configs/channels.yml`. You can copy the example:

```bash
cp configs/channels.example.yml configs/channels.yml
```

Telegram creates a `.session` file during first login. Treat it like a password.

