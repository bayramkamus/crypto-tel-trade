# Installation

1. Create a virtual environment.
2. Install `requirements.txt`.
3. Copy `.env.example` to `.env`.
4. Copy `configs/channels.example.yml` to `configs/channels.yml`.
5. Fill your own Telegram API credentials and channel list.

Optional chart pattern support requires:

```bash
pip install -r requirements-ml.txt
```

